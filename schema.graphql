type Chatroom {
  name: String!
  message: [Message!]!
}

type Message {
  id: ID!
  text: String!
}

type Query{
  room(name: String!): Chatroom
}

type Mutation{
  postMessage(input: NewMessage!): Message!
}

input NewMessage{
  text: String!
  roomName: String!
}
