type Chatroom {
  name: String!
  message: [Message!]!
  id: ID!
}

type Message {
  text: String!
  id: ID!
}

type Query{
  room(name: String!): Chatroom!
  rooms: Chatroom!
}

type Mutation{
  postMessage(text: String!,roomName: String!): Message
}

